#include "MCJitTestInclude.wns"

struct LinkedListNode {
	Int x = 4;
	LinkedListNode next = ~;
}

Int test(Int q, Int z) {
	LinkedListNode head = LinkedListNode();
	LinkedListNode tail = head;
	head.x = 0;
	Int i = 0;
	
	while(i < 100) {
		++i;
		tail.next = LinkedListNode();
		tail = tail.next;
		tail.x = i;
	}
	
	tail = head;
	while(tail.next != ~) {
		tail = tail.next;
		i = tail.x;
	}
	return(i);
}
/*

Int test(Int q, Int z) {
	LinkedListNode a = LinkedListNode();
	LinkedListNode b = ~;
	b <== a;
	a = ~;
	if(b == ~) {
		return(-1);
	}
	return(b.x);
}

Void test3() {
	return;
}

Int test(Int a, Int b) {
	Int[][] x = Int[12][12]();
	Int i = 0;
	Int j = 0;
	while(i < 12)
	{
		while(j < 12)
		{
			x[i][j] = i*j;
			j++;
		}
		i++;
		j=0;
	}
	test3();
	return(x[11][11]);
}

Int test(Int a, Int b) {
	Int x = 0;
	Int i = 0; 
	for(;;) {
		if(-(++i < 10)) {
			return(x);
		}
		x = x + i;
		
	}
	return(x);
}
*/